\documentclass[openany]{memoir}

\usepackage{subfiles}

% Open Logic main path is two directory levels up; parts live in content/*/
\newcommand{\olpath}{../..}

% For a localization, put this file in `locale/<langid>/include/`
% Comment out the line above, and uncomment the folllowing line
% \newcommand*{\olpath}{../../../../..}

\input{\olpath/sty/open-logic.sty}
\input{\olpath/sty/open-logic-debug.sty}

% For a localization, uncomment the following line and replace <langid>
% \input{\olpath/locale/<langid>/open-logic-locale.sty}

\pagestyle{openlogic}
  
\begin{document}

This file should not be compiled on its own. It is merely a template to
compile parts by themselves.

\end{document}

